pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello Jenkins!'
            }
        }

        stage('Date') {
            steps {
                sh 'date'
            }
        }
    }
}
